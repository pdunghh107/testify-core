spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:${POSTGRES_PORT:5432}}/${DB_NAME:${POSTGRES_DB:testify_db}}
    username: ${DB_USER:${POSTGRES_USER:fiza_sit}}
    password: ${DB_PASSWORD:${POSTGRES_PASSWORD:L2mhoangvinh@83}}
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    create-schemas: true
  rabbitmq:
    host: ${RABBITMQ_HOST:rabbitmq}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:zcomini}
    password: ${RABBITMQ_PASSWORD:zcomini}

app:
  auth:
    expose-dev-secrets: false
